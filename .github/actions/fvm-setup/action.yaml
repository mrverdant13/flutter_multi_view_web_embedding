name: FVM Setup
description: Installs FVM and adds it to the PATH

runs:
  using: composite
  steps:
    - name: Install FVM
      shell: bash
      run: curl -fsSL https://fvm.app/install.sh | bash
    - name: Add FVM to PATH
      shell: bash
      run: echo "$HOME/fvm/bin" >> "$GITHUB_PATH"
